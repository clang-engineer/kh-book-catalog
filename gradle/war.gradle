apply plugin: "war"

bootWar {
    mainClass = "com.clangengineer.bookcatalog.BookCatalogApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
